name: Complete Platform Deployment (IAM Role)

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy'
        required: true
        default: 'test'
        type: choice
        options:
          - test
          - prod
      deploy_infrastructure:
        description: 'Deploy Terraform infrastructure'
        required: true
        default: true
        type: boolean
      deploy_global_components:
        description: 'Deploy global components'
        required: true
        default: true
        type: boolean
      deploy_argocd:
        description: 'Deploy ArgoCD'
        required: true
        default: true
        type: boolean

env:
  AWS_REGION: ${{ secrets.AWS_REGION }}
  CLUSTER_NAME: duka-eks-cluster

jobs:
  deploy-infrastructure:
    name: Deploy Terraform Infrastructure
    runs-on: ubuntu-latest
    if: github.event.inputs.deploy_infrastructure == 'true'
    environment: ${{ github.event.inputs.environment }}
    permissions:
      id-token: write
      contents: read
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/github-actions-dukqa-role
        role-session-name: GitHubActions-Infrastructure
        aws-region: ${{ secrets.AWS_REGION }}

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.6.0

    - name: Terraform Init
      working-directory: ./DUKA-IAC-TERRAFORM
      run: terraform init

    - name: Terraform Plan
      working-directory: ./DUKA-IAC-TERRAFORM
      run: |
        ENV=${{ github.event.inputs.environment }}
        terraform plan -var-file=environments/${ENV}.tfvars -out=tfplan

    - name: Terraform Apply
      working-directory: ./DUKA-IAC-TERRAFORM
      run: terraform apply tfplan

    - name: Update kubeconfig
      run: |
        aws eks update-kubeconfig --region ${{ env.AWS_REGION }} --name ${{ env.CLUSTER_NAME }}

    - name: Verify cluster
      run: |
        kubectl cluster-info
        kubectl get nodes

  deploy-global-components:
    name: Deploy Global Components
    runs-on: ubuntu-latest
    needs: deploy-infrastructure
    if: always() && (needs.deploy-infrastructure.result == 'success' || needs.deploy-infrastructure.result == 'skipped') && github.event.inputs.deploy_global_components == 'true'
    permissions:
      id-token: write
      contents: read
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/github-actions-dukqa-role
        role-session-name: GitHubActions-GlobalComponents
        aws-region: ${{ secrets.AWS_REGION }}

    - name: Setup kubectl
      uses: azure/setup-kubectl@v3
      with:
        version: 'v1.28.0'

    - name: Update kubeconfig
      run: |
        aws eks update-kubeconfig --region ${{ env.AWS_REGION }} --name ${{ env.CLUSTER_NAME }}

    - name: Deploy Global Components (Ordered)
      run: |
        echo "🚀 Deploying global components in correct order..."
        
        # 1. Namespaces first
        echo "1️⃣ Deploying namespaces..."
        kubectl apply -f cluster-global-components/security/namespaces.yaml
        
        # 2. RBAC second
        echo "2️⃣ Deploying RBAC..."
        kubectl apply -f cluster-global-components/rbac/
        sleep 10
        
        # 3. Storage third
        echo "3️⃣ Deploying storage..."
        kubectl apply -f cluster-global-components/storage/
        sleep 15
        
        # 4. Monitoring last
        echo "4️⃣ Deploying monitoring..."
        kubectl apply -f cluster-global-components/monitoring/
        
        echo "✅ Global components deployment completed"

  deploy-argocd:
    name: Deploy ArgoCD
    runs-on: ubuntu-latest
    needs: deploy-global-components
    if: always() && needs.deploy-global-components.result == 'success' && github.event.inputs.deploy_argocd == 'true'
    permissions:
      id-token: write
      contents: read
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/github-actions-dukqa-role
        role-session-name: GitHubActions-ArgoCD
        aws-region: ${{ secrets.AWS_REGION }}

    - name: Setup kubectl
      uses: azure/setup-kubectl@v3
      with:
        version: 'v1.28.0'

    - name: Update kubeconfig
      run: |
        aws eks update-kubeconfig --region ${{ env.AWS_REGION }} --name ${{ env.CLUSTER_NAME }}

    - name: Install ArgoCD Core
      run: |
        echo "🚀 Installing ArgoCD core components..."
        kubectl apply -n argocd -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml

    - name: Wait for ArgoCD
      run: |
        echo "⏳ Waiting for ArgoCD to be ready..."
        kubectl wait --for=condition=available --timeout=300s deployment/argocd-server -n argocd

    - name: Deploy ArgoCD Applications
      working-directory: ./production-cluster/cluster-manifests/argocd
      run: |
        echo "🚀 Deploying ArgoCD applications..."
        kubectl apply -f argocd-project.yaml
        kubectl apply -f argocd-apps.yaml
        kubectl apply -f argocd-image-updater.yaml

    - name: Get ArgoCD Admin Password
      run: |
        echo "🔑 ArgoCD Admin Password:"
        kubectl -n argocd get secret argocd-initial-admin-secret -o jsonpath="{.data.password}" | base64 -d
        echo ""
        echo "🌐 Access ArgoCD: kubectl port-forward svc/argocd-server -n argocd 8080:443"
        echo "👤 Username: admin"