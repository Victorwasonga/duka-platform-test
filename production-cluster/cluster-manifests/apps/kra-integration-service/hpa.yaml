apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: kra-integration-hpa
  namespace: dukqa-apps
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: kra-integration-deployment
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60

